import 'package:flutter/material.dart';

class NewMessages extends StatelessWidget {
  const NewMessages({super.key});

  @override
  Widget build(BuildContext context) {
    return Text('New Messages');  }
}
